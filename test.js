// Pick the nodes
// give them an X and Y coord
// establish how big the whole grid is

// Grid is 100 units x 100 units
// Each node has an x and a y

// node object has X and Y values
// as well as connections?
    // connections have destination and distance to the given node

function buildGraph(nodes) {
    for (node in nodes) {
        // create a route to each other node
        // ensure we don't create a route to our own node
        // compute distance to other nodes
    }
}

function shortestRoute(graph) {
    // Given the start point, we...
    // Search through the tree to get the shortest possible journey for all the given points

    // Add up all the distances for the routes to make that shit happen baybee
}